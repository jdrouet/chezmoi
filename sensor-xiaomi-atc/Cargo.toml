[package]
name = "chezmoi-sensor-xiaomi-atc"
version = "0.1.0"
edition = "2021"
authors.workspace = true
license-file.workspace = true

[features]
default = ["agent", "web"]
agent = [
    "dep:anyhow",
    "dep:bluer",
    "dep:serde",
    "dep:tokio",
    "chezmoi-sensor-prelude/agent",
]
web = ["dep:another-html-builder", "dep:chezmoi-web-prelude"]

[dependencies]
chezmoi-entity = { path = "../entity", features = ["serde"] }
chezmoi-sensor-prelude = { path = "../sensor-prelude" }
chezmoi-web-prelude = { path = "../web-prelude", optional = true }

another-html-builder = { version = "0.2", optional = true }
anyhow = { workspace = true, optional = true }
bluer = { workspace = true, features = [
    "bluetoothd",
    "serde",
], default-features = false, optional = true }
serde = { workspace = true, features = ["derive"], optional = true }
tokio = { workspace = true, features = ["sync", "time"], optional = true }
tracing = { workspace = true }
